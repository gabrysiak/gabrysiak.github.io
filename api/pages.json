---
layout: null
---

{
  "pages": [
    {% for page in site.pages %}{
      "title": "{{ page.title }}",
      "url": "{{ page.url | prepend: site.baseurl }}",
      "order": "{{ page.order }}"
    }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
}